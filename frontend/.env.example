
NEXT_PUBLIC_SUPABASE_URL=

NEXT_PUBLIC_SUPABASE_ANON_KEY=

SUPABASE_SERVICE_ROLE_KEY=


NEXT_PUBLIC_SITE_URL=

#############################################
# Local dev: TLS / certificate errors (Windows)
#############################################
#
# If you see "fetch failed" or "UNABLE_TO_GET_ISSUER_CERT_LOCALLY" when
# submitting the waitlist form, use ONE of these (local dev only):
#
# Option A – Insecure dev fetch (Supabase requests only, recommended):
#   SUPABASE_INSECURE_DEV=1
#   Then run: npm run dev
#
# Option B – Process-wide TLS relax (only if A doesn’t work):
#   Run: npm run dev:win
#   (Do not set NODE_TLS_REJECT_UNAUTHORIZED in production.)
#
# Option C – Corporate proxy / custom CA:
#   NODE_EXTRA_CA_CERTS=C:\path\to\your\corporate-root.pem
#   Then run: npm run dev
#
# See LOCAL_DEVELOPMENT.md for full instructions.
